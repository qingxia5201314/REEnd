# **数据导入与导出的简单回顾**
- 导出一张表的数据
  - 相对路径下的导出文件位置，看配置文件设置
  ```
    select deptno,dname,loc
    from dept
    into outfile 'e:/1/1.txt';
    ——————
    select empno,ename,job,hiredate,deptno
    from emp
    into outfile 'emp1.txt';
    ```
- 导出一个视图的数据

    ```
    select e.*,d.*
    from emp e,dept d
    where e.deptno=d.deptno
    into outfile 'e:/1/deptno.txt'
    ```
- 导入一张表的数据
  1. 创建一张空表T1，结构和emp一致

       ```
        create table t1 like emp;
       ```
    2. 导入数据：**要求表结构兼容数据**
        ```
        load data local infile 'e:/1/3.txt'
        into table t1;
        ```


        --local-infile
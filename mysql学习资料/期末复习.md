# 期末复习

## 1. MySQL约束 (Constraints)
**核心考点**：掌握建表时的语法，以及每个约束对数据完整性的作用。

- **NULL / NOT NULL (空/非空约束)**
  - 作用：规定字段是否可以为空。主键默认是非空的。
- **DEFAULT (默认值约束)**
  - 作用：当插入数据未指定该字段值时，使用默认值。
- **COMMENT (注释)**
  - 作用：为字段添加说明，不影响逻辑，但利于维护。
- **ZEROFILL (零填充)**
  - 作用：配合整型使用，长度不足时左侧补0（如 `001`）。
- **PRIMARY KEY (主键约束)**
  - 作用：**唯一**标识一条记录，**非空**且**唯一**。一张表只能有一个主键。
- **AUTO_INCREMENT (自增长约束)**
  - 作用：系统自动分配递增的序列号，通常配合主键使用。
- **UNIQUE KEY (唯一约束)**
  - 作用：保证字段值不重复（允许为空 NULL）。
- **FOREIGN KEY (外键约束)**
  - 作用：保持数据一致性，将当前表与父表主键关联。

> **代码示例**：
> ```sql
> CREATE TABLE student (
>     id INT PRIMARY KEY AUTO_INCREMENT,
>     name VARCHAR(20) NOT NULL,
>     phone VARCHAR(11) UNIQUE KEY,
>     class_id INT,
>     FOREIGN KEY (class_id) REFERENCES class(id)
> );
> ```

---

## 2. 数据库设计的六个基本步骤
**核心考点**：简答题常考，需背诵顺序。

1. **需求分析阶段**：分析用户需求（产出数据流图、数据字典）。
2. **概念结构设计阶段**：将需求转化为 **E-R 图**（实体-联系图）。
3. **逻辑结构设计阶段**：将 E-R 图转换为**关系模式**（数据库表），进行范式规范化。
4. **物理设计阶段**：选择存储引擎、索引策略等。
5. **数据库实施阶段**：编写 SQL 建库建表，载入数据。
6. **数据库运行和维护阶段**：性能优化、备份、安全管理。

---

## 3. MySQL 数据库类型
- **类型**：**RDBMS** (Relational Database Management System，关系型数据库管理系统)。
- **特点**：数据以**表**的形式存储，通过公共字段建立联系。

---

## 4. MySQL的七个查询语句 (书写 vs 执行)
**核心考点**：**执行顺序**是选择题和面试的重灾区。

| 关键字 | 含义 | 书写顺序 | **执行顺序 (重点)** |
| :--- | :--- | :--- | :--- |
| **FROM** | 数据来源 | 2 | **1** |
| **WHERE** | 分组前过滤 | 3 | **2** |
| **GROUP BY** | 分组 | 4 | **3** |
| **HAVING** | 分组后过滤 | 5 | **4** |
| **SELECT** | 选择列 | 1 | **5** |
| **ORDER BY** | 排序 | 6 | **6** |
| **LIMIT** | 限制行数 | 7 | **7** |

> **注意**：`WHERE` 不能接聚合函数（sum, avg），`HAVING` 可以。

---

## 5. MySQL的数据类型
- **数值类型**：`INT` (整数), `DECIMAL` (定点数/金额), `FLOAT` (浮点)。
- **日期和时间类型**：`DATE` (日期), `DATETIME` (日期时间), `TIMESTAMP` (时间戳)。
- **字符串类型**：`CHAR` (定长/快), `VARCHAR` (变长/省空间), `TEXT` (长文本)。
- **枚举与集合类型**：`ENUM` (单选), `SET` (多选)。
- **空间数据类型**：GIS相关类型（考试较少涉及）。

---

## 6. 事务的四大特性 (ACID)
**核心考点**：必须背诵定义的关键词。

- **原子性 (Atomicity)**：事务是最小单位，要么全部成功，要么全部失败（回滚）。
- **一致性 (Consistency)**：事务前后数据状态保持守恒/一致。
- **隔离性 (Isolation)**：一个事务不应被其他事务干扰（涉及脏读、幻读）。
- **持久性 (Durability)**：事务提交后，修改是永久的（写入磁盘）。

---

## 7. 数据库表关系
- **一对一 (1:1)**
  - 实现：外键放在任意一方，并设为 `UNIQUE`。
- **一对多 (1:N)**
  - 例子：班级 -> 学生。
  - 实现：**外键放在“多”的一方**（学生表存班级ID）。
- **多对多 (N:M)**
  - 例子：学生 <-> 课程。
  - 实现：**必须创建第三张“中间表”**，包含两个表的主键作为联合主键。

---

## 8. SQL 实战演练 (基于 emp/dept 表)

### 8.1 限制查询 (LIMIT) - 分页查询
> **考点**：`LIMIT 起始下标, 长度`。注意下标从 0 开始。

```sql
-- 1. 查找emp中的所有信息，按hiredate升序排序，从第5条记录开始共6条
SELECT * FROM emp ORDER BY hiredate ASC LIMIT 4, 6;

-- 2. 查找emp中的所有信息，按sal降序排序，取前5名
SELECT * FROM emp ORDER BY sal DESC LIMIT 5;
-- 3. 查找“最高薪”和“最低薪”
SELECT MAX(sal) AS 最高薪, MIN(sal) AS 最低薪 FROM emp;

-- 4. 查找“所有人平均年薪”和“所有人的总年薪” (假设sal为月薪)
SELECT AVG(sal*12) AS 平均年薪, SUM(sal*12) AS 总年薪 FROM emp;

-- 5. 查找empno列的记录数 (总人数)
SELECT COUNT(empno) FROM emp;
-- 6. 查找每个部门的：部门号，部门平均工资，部门最高工资
SELECT deptno, AVG(sal), MAX(sal) FROM emp GROUP BY deptno;

-- 7. 多重分组：按部门号分组的基础上，再按工作类别分组
SELECT deptno, job, AVG(sal), MAX(sal) FROM emp GROUP BY deptno, job; -- 这里为什么要分组呢？因为deptno和job是列，avg(sal),max(sal)是一个数值，这两个有不确定性，不能一一对应，所以需要分组来确定每个部门和工作的平均工资和最高工资

-- 8. 分组后过滤：查找平均工资小于2000的部门
SELECT deptno, AVG(sal) FROM emp 
GROUP BY deptno 
HAVING AVG(sal) < 2000;

-- 9. 综合排序：查找部门平均工资 > 2000 的部门，并按平均工资降序排序
SELECT deptno, AVG(sal) AS avg_sal 
FROM emp 
GROUP BY deptno 
HAVING avg_sal > 2000 
ORDER BY avg_sal DESC;
-- 10. 查找 ename, dname 信息 (笛卡尔积/隐式内连接)
SELECT e.ename, d.dname FROM emp e, dept d WHERE e.deptno = d.deptno;

-- 11. 标准内连接：查找 ename, sal, dname, deptno
SELECT e.ename, e.sal, d.dname, e.deptno 
FROM emp e 
JOIN dept d ON e.deptno = d.deptno;

-- 12. 连接后过滤：查找部门号=10的 dname, ename, sal, deptno
SELECT d.dname, e.ename, e.sal, e.deptno 
FROM emp e 
JOIN dept d ON e.deptno = d.deptno 
WHERE e.deptno = 10;

-- 13. 连接后排序：先按部门号排序，再按工资排序
SELECT e.ename, e.sal, d.dname 
FROM emp e 
JOIN dept d ON e.deptno = d.deptno 
ORDER BY e.deptno, e.sal;

-- 14. 自连接：查找员工名字和他上司的名字
SELECT e.ename AS 员工, m.ename AS 上司 
FROM emp e 
JOIN emp m ON e.mgr = m.empno;
-- 15. 查找最高工资员工的信息 (标量子查询)
SELECT * FROM emp WHERE sal = (SELECT MAX(sal) FROM emp);

-- 16. 查找大于平均工资的所有员工信息
SELECT * FROM emp WHERE sal > (SELECT AVG(sal) FROM emp);

-- 17. 查找 'SMITH' 所在部门的所有员工名字
SELECT ename FROM emp 
WHERE deptno = (SELECT deptno FROM emp WHERE ename = 'SMITH');

-- 18. IN 子查询：查找工作类型在部门10中有的员工，但排除部门10本身的员工
SELECT * FROM emp 
WHERE job IN (SELECT job FROM emp WHERE deptno = 10) 
AND deptno != 10;

-- 19. 相关子查询 (难点)：查找比【本部门】平均工资高的员工
-- 解析：将外表 e1 的 deptno 传给内表 e2 进行过滤
SELECT * FROM emp e1 
WHERE sal > (
    SELECT AVG(sal) FROM emp e2 WHERE e2.deptno = e1.deptno
);
```

---
## 9. 索引 (Index)
**概念**：
索引相当于书籍的**目录**。它是一种特殊的数据结构（如 B+树），帮助数据库系统快速找到对应的数据行，而无需扫描整个表。

**作用 (优缺点)**：
* **优点 (Why use it)**：
    * **极大加快查询速度**（最主要的作用）。
    * 唯一性索引可以保证数据库表中每一行数据的唯一性。
    * 加速表和表之间的连接（JOIN）。
    * 在使用分组（GROUP BY）和排序（ORDER BY）时，显著减少时间。
* **缺点 (Cost)**：
    * **占用物理空间**：索引文件需要存储在磁盘上。
    * **降低增删改速度**：当执行 `INSERT`、`UPDATE`、`DELETE` 时，数据库不仅要修改数据，还要维护索引树，导致性能下降。

**常见类型**：
* 普通索引 (Normal)
* 唯一索引 (Unique)
* 主键索引 (Primary Key)
* 全文索引 (Fulltext)
* 组合索引 (多列索引)

---

## 10. 约束：主键与外键 (PK & FK)
**核心区别与联系**：

| 特性 | **主键 (Primary Key)** | **外键 (Foreign Key)** |
| :--- | :--- | :--- |
| **定义** | 唯一标识表中的一条记录。 | 用于建立两个表之间的联系。 |
| **唯一性** | 值必须**唯一**。 | 值可以**重复**。 |
| **空值** | **不允许为空 (NOT NULL)**。 | **允许为空 (NULL)**（除非额外指定）。 |
| **数量** | 一个表**只能有一个**主键 (可以是复合主键)。 | 一个表可以有**多个**外键。 |
| **作用** | 保证自身数据的完整性。 | 保证参照完整性 (Referential Integrity)。 |
| **关系** | 通常是“父表”的身份证。 | 通常在“子表”中引用父表的主键。 |

> **考试口诀**：主键是“身份证号”（一人一个，不能丢），外键是“籍贯代码”（指向另一个标准的表，可以重复，可以空着）。

---

## 11. 数据库备份的分类
备份通常可以从**物理/逻辑**、**在线/离线**、**备份量**三个维度来划分。

### 11.1 按备份策略/量划分 (最常考)
1.  **完全备份 (Full Backup)**
    * 备份整个数据库的所有数据。
    * *特点*：恢复最快，但备份时间长，占用空间大。
2.  **差异备份 (Differential Backup)**
    * 备份自**上一次完全备份**之后有变化的数据。
    * *特点*：备份速度中等，恢复时需要“全备+最后一次差备”。
3.  **增量备份 (Incremental Backup)**
    * 备份自**上一次备份**（无论是全备、差备还是增备）之后有变化的数据。
    * *特点*：备份最快，占用空间最小，但恢复最麻烦（需要按顺序还原所有备份链）。

### 11.2 按备份实现方式划分
1.  **物理备份**
    * 直接复制数据库的物理文件（如 `.ibd` 文件, log 文件）。
    * *工具*：Xtrabackup, cp/tar 命令。
    * *特点*：速度极快，适合大数据量。
2.  **逻辑备份**
    * 将数据导出为 SQL 语句（CREATE, INSERT）。
    * *工具*：`mysqldump`。
    * *特点*：速度慢，文件可读，适合跨版本/跨平台迁移。

### 11.3 按数据库状态划分
1.  **冷备份 (Cold/Offline Backup)**
    * **关机**备份。停止数据库服务后拷贝文件。
    * *特点*：最安全，但业务会中断。
2.  **热备份 (Hot/Online Backup)**
    * **开机**备份。数据库运行中进行备份，不影响读写。
    * *特点*：需要专业工具支持（如 InnoDB 引擎支持热备），业务不中断。

---

<!-- ## 基础查询
1. 查询所有员工的姓名和工资
2. 查询所有员工的姓名，工作和入职日期，并按入职日期降序排序
3. 查询部门编号为30的所有员工信息
4. 查询工资大于2000的员工姓名和工资
5. 查询工作为'MANAGER'或'ANALYST'的员工姓名和工作
6. 查询姓名以's'开头的员工星系
7. 查询没有佣金(comm)的员工姓名和佣金
8. 查询工资在1000到2000之间的员工姓名和工资
9. 查询所有部门的名称
10. 查询员工总数 -->
# 期末部分题库
## 基础查询实战

**1. 查询所有员工的姓名和工资**
```sql
SELECT ename, sal FROM emp;
```

**2. 查询所有员工的姓名，工作和入职日期，并按入职日期降序排序**
```sql
SELECT ename, job, hiredate 
FROM emp 
ORDER BY hiredate DESC;
```

**3. 查询部门编号为30的所有员工信息**
```sql
SELECT * FROM emp WHERE deptno = 30;
```

**4. 查询工资大于2000的员工姓名和工资**
```sql
SELECT ename, sal FROM emp WHERE sal > 2000;
```

**5. 查询工作为 'MANAGER' 或 'ANALYST' 的员工姓名和工作**
```sql
SELECT ename, job 
FROM emp 
WHERE job IN ('MANAGER', 'ANALYST');
```

**6. 查询姓名以 'S' 开头的员工信息**
> *注：通常数据库区分大小写，若名字是大写存储请用 'S%'*
```sql
SELECT * FROM emp WHERE ename LIKE 'S%';
```

**7. 查询没有佣金(comm)的员工姓名和佣金**
```sql
SELECT ename, comm FROM emp WHERE comm IS NULL;
```

**8. 查询工资在1000到2000之间的员工姓名和工资**
```sql
SELECT ename, sal 
FROM emp 
WHERE sal BETWEEN 1000 AND 2000;
```

**9. 查询所有部门的名称**
```sql
SELECT dname FROM dept;
```

**10. 查询员工总数**
```sql
SELECT COUNT(*) AS 员工总数 FROM emp;
```

<!-- ## 多表连接查询
1. 查询每个员工的姓名、部门编号和部门名称
2. 查询部门所在地为'DDALLAS'的所有员工姓名
3. 查询每个员工的姓名、工资以及对应的工资等级
4. 查询'SALES'部门的所有员工姓名和工资
5. 查询工资等级为4的所有员工姓名和工资
6. 查询每个员工的姓名、工资、部门名称以及工资等级
7. 查询员工'KING'所在部门的所有员工信息
8. 查询每个部门的部门名称和该部门的员工人数
9. 查询每个部门的部门名称和改不萌的平均工资
10. 查询员工人数多于3人的部门名称和员工人数 -->

## 多表连接查询实战

**1. 查询每个员工的姓名、部门编号和部门名称**
> *考点：标准内连接 (Inner Join)*
```sql
SELECT e.ename, e.deptno, d.dname 
FROM emp e 
JOIN dept d ON e.deptno = d.deptno;
```

**2. 查询部门所在地为 'DALLAS' 的所有员工姓名**
> *考点：连接查询 + WHERE 过滤*
```sql
SELECT e.ename 
FROM emp e 
JOIN dept d ON e.deptno = d.deptno 
WHERE d.loc = 'DALLAS';
```

**3. 查询每个员工的姓名、工资以及对应的工资等级**
> *考点：非等值连接 (Non-Equi Join)，连接条件不是相等，而是区间*
```sql
SELECT e.ename, e.sal, s.grade 
FROM emp e 
JOIN salgrade s ON e.sal BETWEEN s.losal AND s.hisal;
```

**4. 查询 'SALES' 部门的所有员工姓名和工资**
```sql
SELECT e.ename, e.sal 
FROM emp e 
JOIN dept d ON e.deptno = d.deptno 
WHERE d.dname = 'SALES';
```

**5. 查询工资等级为 4 的所有员工姓名和工资**
```sql
SELECT e.ename, e.sal 
FROM emp e 
JOIN salgrade s ON e.sal BETWEEN s.losal AND s.hisal 
WHERE s.grade = 4;
```

**6. 查询每个员工的姓名、工资、部门名称以及工资等级**
> *考点：多表联合 (3张表连接)*
```sql
SELECT e.ename, e.sal, d.dname, s.grade 
FROM emp e 
JOIN dept d ON e.deptno = d.deptno 
JOIN salgrade s ON e.sal BETWEEN s.losal AND s.hisal;
```

**7. 查询员工 'KING' 所在部门的所有员工信息**
> *考点：自连接 (Self-Join) 或 子查询。此处展示自连接写法*
```sql
SELECT e1.* FROM emp e1 
JOIN emp e2 ON e1.deptno = e2.deptno 
WHERE e2.ename = 'KING';
```

**8. 查询每个部门的部门名称和该部门的员工人数**
> *考点：连接 + 分组 (GROUP BY) + 聚合函数*
```sql
SELECT d.dname, COUNT(e.empno) AS 员工人数 
FROM dept d 
JOIN emp e ON d.deptno = e.deptno 
GROUP BY d.dname;
```

**9. 查询每个部门的部门名称和该部门的平均工资**
```sql
SELECT d.dname, AVG(e.sal) AS 平均工资 
FROM dept d 
JOIN emp e ON d.deptno = e.deptno 
GROUP BY d.dname;
```

**10. 查询员工人数多于 3 人的部门名称和员工人数**
> *考点：分组后的过滤 (HAVING)*
```sql
SELECT d.dname, COUNT(e.empno) AS 员工人数 
FROM dept d 
JOIN emp e ON d.deptno = e.deptno 
GROUP BY d.dname 
HAVING COUNT(e.empno) > 3;
```


# 数据库期末复习题库 (错题精编版)

---

## 一、 单项选择题

1. 下列哪种数据类型适合存储日期和时间（ B）
   A. DATE
   B. DATETIME
   C. TIME
   D. YEAR
> **参考答案**：B

**2. ❌ [重点复习] MySQL 中，用于修改表字段数据类型的语句是（B ）**
   A. ALTER TABLE 表名 MODIFY 字段名 新类型
   B. ALTER TABLE 表名 CHANGE 字段名 新类型
   C. UPDATE TABLE 表名 SET 字段名 类型 = 新类型
   D. MODIFY TABLE 表名 字段名 新类型
> **参考答案**：A
> **解析**：虽然 `CHANGE` 也可以改类型，但它主要用于“改名+改类型”。只修改类型通常使用 `MODIFY`。若单选，A 更准确。

3. 以下哪个语句用于创建唯一索引（A ）
   A. CREATE UNIQUE INDEX 索引名 ON 表名 (字段名)
   B. ADD UNIQUE INDEX 索引名 TO 表名 (字段名)
   C. CREATE INDEX UNIQUE 索引名 IN 表名 (字段名)
   D. ALTER TABLE 表名 ADD UNIQUE 索引名 (字段名)
> **参考答案**：A

4. 事务提交的 SQL 语句是（ A）
   A. COMMIT
   B. SUBMIT
   C. SAVE
   D. FINISH
> **参考答案**：A

5. 下列哪种连接会返回两个表中所有匹配的记录（ D）
   A. 左外连接
   B. 右外连接
   C. 内连接
   D. 全外连接
> **参考答案**：C (注：题目问“匹配的记录”，内连接只返回匹配的；全外连接返回匹配+不匹配的。若题目意为“仅匹配”，选C；若意为“包括所有”，选D。结合常考点，内连接C可能性大，你的选项D全外连接通常包含不匹配的NULL)

6. MySQL 中，查看当前数据库的语句是（ A）
   A. SHOW DATABASE
   B. SELECT DATABASE ()
   C. VIEW DATABASE
   D. USE DATABASE
> **参考答案**：B (解析见填空题部分)

7. 用于修改表中字段名称的语句是（ B）
   A. ALTER TABLE 表名 RENAME COLUMN 旧字段名 TO 新字段名
   B. ALTER TABLE 表名 CHANGE 旧字段名 新字段名 类型
   C. UPDATE TABLE 表名 RENAME 旧字段名 新字段名
   D. CHANGE TABLE 表名 旧字段名 新字段名
> **参考答案**：B

8. 以下聚合函数用于统计非空值个数的是（B ）
   A. COUNT (*)
   B. COUNT (字段名)
   C. SUM ()
   D. AVG ()
> **参考答案**：B (COUNT(*)统计所有行含NULL，COUNT(字段)只统计非空)

9. 若要查询工资大于 3000 且小于 5000 的员工，错误的条件是（ D）
   A. WHERE sal > 3000 AND sal < 5000
   B. WHERE sal BETWEEN 3001 AND 4999
   C. WHERE sal IN (3000,5000)
   D. WHERE sal NOT BETWEEN 0 AND 3000 AND sal NOT BETWEEN 5000 AND 10000
> **参考答案**：C (IN 是等于其中任意一个，不是区间)

10. MySQL 中，默认的字符集是（B ）
    A. GBK
    B. UTF-8
    C. Latin1
    D. UTF-16
> **参考答案**：B (新版MySQL默认为utf8mb4)

11. 下列关于视图的说法正确的是（ C）
    A. 视图可以存储数据
    B. 视图只能基于单张表创建
    C. 视图可以被更新，前提是符合更新规则
    D. 视图创建后不能删除
> **参考答案**：C

12. 用于删除数据库的语句是（A ）
    A. DROP DATABASE 数据库名
    B. DELETE DATABASE 数据库名
    C. REMOVE DATABASE 数据库名
    D. ERASE DATABASE 数据库名
> **参考答案**：A

13. 以下哪种约束可以确保字段值不重复且不为空（B ）
    A. 唯一约束
    B. 主键约束
    C. 非空约束
    D. 检查约束
> **参考答案**：B

14. MySQL 中，查看所有数据库的语句是（ A）
    A. SHOW DATABASES
    B. LIST DATABASES
    C. VIEW ALL DATABASES
    D. SELECT ALL DATABASES
> **参考答案**：A

15. 若要在 emp 表中添加 “age” 字段（INT 类型，非空），正确的语句是（ A）
    A. ALTER TABLE emp ADD age INT NOT NULL
    B. ADD COLUMN age INT NOT NULL TO emp
    C. ALTER TABLE emp INSERT age INT NOT NULL
    D. INSERT COLUMN age INT NOT NULL INTO emp
> **参考答案**：A

16. 下列哪种事务隔离级别会产生幻读（B ）
    A. Read Uncommitted
    B. Read Committed
    C. Repeatable Read
    D. Serializable
> **参考答案**：B (及 A)

17. 用于查询表中前 10 条记录的语句是（B ）
    A. SELECT TOP 10 * FROM 表名
    B. SELECT * FROM 表名 LIMIT 10
    C. SELECT * FROM 表名 LIMIT 0,10
    D. SELECT * FROM 表名 ROWS 10
> **参考答案**：B

18. MySQL 中，存储过程的创建关键字是（ B）
    A. CREATE PROCEDURE
    B. CREATE FUNCTION
    C. CREATE PROCESS
    D. CREATE ROUTINE
> **参考答案**：A (你的答案选了B，B是函数)

19. 下列关于索引的说法错误的是（ D）
    A. 索引可以提高查询速度
    B. 索引会降低插入、更新、删除的速度
    C. 主键自动创建索引
    D. 索引越多越好
> **参考答案**：D

**20. ❌ [重点复习] 若要查询 “姓名以张开头” 的员工信息，正确的条件是（ A）**
    A. WHERE name LIKE ' 张 %'
    B. WHERE name LIKE '% 张 '
    C. WHERE name LIKE '% 张 %'
    D. WHERE name = ' 张 %'
> **参考答案**：A
> **解析**：`%` 在后表示以此开头，后面接任意字符。注意题目代码块里可能有空格干扰，逻辑上选A。

21. MySQL 中，用于查看索引的语句是（A ）
    A. SHOW INDEX FROM 表名
    B. VIEW INDEX ON 表名
    C. SELECT INDEX FROM 表名
    D. DESCRIBE INDEX 表名
> **参考答案**：A

22. 下列哪种数据类型适合存储大文本数据（ B）
    A. VARCHAR
    B. TEXT
    C. CHAR
    D. BLOB
> **参考答案**：B (BLOB存二进制，TEXT存文本)

23. 事务回滚的 SQL 语句是（ D）
    A. ROLLBACK
    B. CANCEL
    C. UNDO
    D. REVERT
> **参考答案**：A (你的答案选了D，应为 ROLLBACK)

24. 若要修改表中数据，正确的语句是（A ）
    A. UPDATE 表名 SET 字段名 = 值 WHERE 条件
    B. MODIFY 表名 SET 字段名 = 值 WHERE 条件
    C. CHANGE 表名 字段名 = 值 WHERE 条件
    D. ALTER 表名 SET 字段名 = 值 WHERE 条件
> **参考答案**：A

25. MySQL 中，存储过程和函数的区别是（ A）
    A. 函数可以有返回值，存储过程没有
    B. 存储过程可以有返回值，函数没有
    C. 函数不能带参数，存储过程可以
    D. 存储过程不能带参数，函数可以
> **参考答案**：A (严谨说法：函数必须有返回值，存储过程通过out参数返回)

26. 下列关于外键的说法正确的是（ A）
    A. 外键必须关联到另一张表的主键
    B. 外键可以关联到另一张表的任意字段
    C. 一张表只能有一个外键
    D. 外键字段的值可以为 NULL
> **参考答案**：D (A通常是对的，但外键也可以关联到Unique Key；D也是对的。若单选通常选A作为定义，但D是外键的特性)

27. 用于查询当前用户的语句是（A ）
    A. SELECT USER ()
    B. SHOW USER
    C. VIEW USER
    D. SELECT CURRENT_USER
> **参考答案**：A

28. MySQL 中，查看表中所有索引的语句是（ A）
    A. SHOW INDEXES FROM 表名
    B. DESCRIBE INDEX 表名
    C. SELECT INDEXES FROM 表名
    D. LIST INDEXES ON 表名
> **参考答案**：A

29. 若要删除表中的所有记录且保留表结构，正确的语句是（B ）
    A. DELETE FROM 表名
    B. TRUNCATE TABLE 表名
    C. DROP TABLE 表名
    D. REMOVE FROM 表名
> **参考答案**：B (DELETE也可以，但TRUNCATE效率更高且重置自增)

30. 下列哪种数据类型适合存储二进制数据（D ）
    A. TEXT
    B. VARCHAR
    C. BLOB
    D. CHAR
> **参考答案**：C (你选了D，BLOB才是Binary Large Object)

31. MySQL 中，设置事务隔离级别的语句是（ A）
    A. SET TRANSACTION ISOLATION LEVEL 级别
    B. SET ISOLATION LEVEL 级别
    C. ALTER TRANSACTION ISOLATION LEVEL 级别
    D. CHANGE TRANSACTION ISOLATION LEVEL 级别
> **参考答案**：A

32. 用于查询表中字段的默认值的语句是（ C）
    A. SHOW COLUMNS FROM 表名 LIKE ' 字段名 '
    B. DESCRIBE 表名 字段名
    C. SELECT DEFAULT (字段名) FROM 表名
    D. VIEW DEFAULT 字段名 FROM 表名
> **参考答案**：A 或 B (通常用 DESC 表名 看 Default 列)

33. 下列关于存储引擎的说法正确的是（ A）
    A. InnoDB 支持事务，MyISAM 不支持
    B. MyISAM 支持事务，InnoDB 不支持
    C. InnoDB 不支持外键，MyISAM 支持
    D. 两者都支持全文索引
> **参考答案**：A

34. 若要查询 “入职日期在 2020 年 1 月 1 日到 2023 年 12 月 31 日之间” 的员工，正确的条件是（ A）
    A. WHERE hiredate BETWEEN '2020-01-01' AND '2023-12-31'
    B. WHERE hiredate > '2020-01-01' AND hiredate < '2023-12-31'
    C WHERE hiredate IN ('2020-01-01','2023-12-31')
    D. WHERE hiredate >= '2020-01-01' OR hiredate <= '2023-12-31'
> **参考答案**：A

35. MySQL 中，用于创建函数的语句是（A ）
    A. CREATE FUNCTION
    B. CREATE PROCEDURE
    C. CREATE FUNCTION PROCEDURE
    D. CREATE ROUTINE FUNCTION
> **参考答案**：A

36. MySQL 中，用于限制查询结果行数的关键字是（ B）
    A. TOP
    B. LIMIT
    C. ROWS
    D. COUNT
> **参考答案**：B

37. 用于查看当前数据库连接数的语句是（A ）
    A. SHOW PROCESSLIST
    B. VIEW CONNECTIONS
    C. SELECT CONNECTION_COUNT ()
    D. LIST CONNECTIONS
> **参考答案**：A

38. MySQL 中，修改用户密码的语句是（ A）
    A. ALTER USER 用户名 IDENTIFIED BY ' 新密码 '
    B. UPDATE USER SET PASSWORD=' 新密码 ' WHERE USER=' 用户名 '
    C. CHANGE USER 用户名 PASSWORD ' 新密码 '
    D. SET PASSWORD FOR 用户名 =' 新密码'
> **参考答案**：A

39. 下列哪种数据类型适合存储货币值（ B）
    A. INT
    B. FLOAT
    C. DECIMAL
    D. DOUBLE
> **参考答案**：C (你选了B，货币必须用DECIMAL定点数防止精度丢失)

40. 若要查询 “部门编号为 10 或 20，且工资大于 4000” 的员工，正确的条件是（ D）
    A. WHERE deptno IN (10,20) AND sal > 4000
    B. WHERE deptno = 10 OR deptno = 20 AND sal > 4000
    C. WHERE deptno BETWEEN 10 AND 20 AND sal > 4000
    D. WHERE deptno = 10 OR (deptno = 20 AND sal > 4000)
> **参考答案**：A (你选了D，逻辑上有歧义，通常用括号包裹OR条件。A最清晰)

41. MySQL 中，用于删除触发器的语句是（ A）
    A. DROP TRIGGER 触发器名
    B. DELETE TRIGGER 触发器名
    C. REMOVE TRIGGER 触发器名
    D. ALTER TRIGGER 触发器名 DROP
> **参考答案**：A

42. 下列关于视图的说法错误的是（D ）
    A. 视图基于查询结果创建
    B. 视图可以简化复杂查询
    C. 视图可以被索引
    D. 视图不能修改基础表数据
> **参考答案**：D (MySQL视图一般不支持索引，C也存疑，但D说"不能修改"太绝对，简单视图可以修改)

43. 用于查看表的创建语句的是（A ）
    A. SHOW CREATE TABLE 表名
    B. DESCRIBE CREATE TABLE 表名
    C. SELECT CREATE TABLE 表名
    D. VIEW CREATE TABLE 表名
> **参考答案**：A

44. MySQL 中，用于创建事件的语句是（C ）
    A. CREATE EVENT
    B. CREATE SCHEDULE
    C. CREATE TASK
    D. CREATE JOB
> **参考答案**：A (你选了C)

45. 下列哪种数据类型的长度是固定的（C ）
    A. VARCHAR
    B. TEXT
    C. CHAR
    D. BLOB
> **参考答案**：C

46. 下列用于插入数据的 SQL 语句是（ A）
    A. INSERT INTO
    B. UPDATE
    C. DELETE
    D. SELECT
> **参考答案**：A

47. MySQL 中，用于启用事件调度器的语句是（ C）
    A. SET GLOBAL event_scheduler = ON
    B. ENABLE EVENT SCHEDULER
    C. START EVENT SCHEDULER
    D. SET EVENT SCHEDULER = 1
> **参考答案**：A (通常用 SET GLOBAL)

48. 下列关于存储过程的说法正确的是（A ）
    A. 存储过程可以调用其他存储过程
    B. 存储过程不能带输出参数
    C. 存储过程不能在事务中执行
    D. 存储过程的代码不能修改
> **参考答案**：A

49. 用于查询数据库版本的语句是（ B）
    A. SELECT VERSION ()
    B. SHOW VERSION
    C. VIEW VERSION
    D. SELECT MySQL_VERSION ()
> **参考答案**：A (你选了B，SHOW一般不接VERSION)

50. 下列关于触发器的说法正确的是（ A）
    A. 触发器可以在 INSERT、UPDATE、DELETE 操作前后触发
    B. 触发器只能在 INSERT 操作后触发
    C. 触发器不能带参数
    D. 一张表只能有一个触发器
> **参考答案**：A

51. 下列关键字中，用于过滤分组后结果的是（ B）
    A. WHERE
    B. HAVING
    C. GROUP BY
    D. ORDER BY
> **参考答案**：B

52. MySQL 中，默认的存储引擎是（B ）
    A. MyISAM
    B. InnoDB
    C. Memory
    D. CSV
> **参考答案**：B

53. 若要查询 “佣金为空” 的员工信息，正确的条件是（ A）
    A. WHERE comm IS NULL
    B. WHERE comm = NULL
    C. WHERE comm = ''
    D. WHERE comm IS NOT NULL
> **参考答案**：A

---

## 二、 判断题

**1. ❌ [重点复习] MySQL 中，DATE 类型可以存储日期和时间信息（ T）**
> **参考答案**：F
> **解析**：DATE 只存日期 (YYYY-MM-DD)，DATETIME 才能存时间。

2. 主键约束的字段值可以为 NULL（ F）
> **参考答案**：F

3. mysqldump命令只能备份单个数据库，无法备份多个数据库（ F）
> **参考答案**：F (可以用 --databases 参数)

4. InnoDB 存储引擎支持事务和外键约束（T ）
> **参考答案**：T

5. TRUNCATE 语句可以删除表结构和数据（F ）
> **参考答案**：F (TRUNCATE 删除数据，保留结构)

6. 创建MySQL用户时，若不指定主机地址，则默认主机为localhost（T ）
> **参考答案**：F (通常默认为 % 即任意主机，需视版本而定，但题目通常考察默认只有localhost能连root的安全性，此题较模糊)

7. MySQL 中，默认的事务隔离级别是 Repeatable Read（ T）
> **参考答案**：T

8. 唯一约束的字段值可以为 NULL，且可以有多个 NULL 值（ F）
> **参考答案**：T (Unique允许Null，且在MySQL中允许多个Null) **注：你的答案F是错的，应为T**

9. 外键约束的字段值必须在参照表的对应字段中存在（T ）
> **参考答案**：T (或者为NULL)

10. 存储过程可以返回多个结果集（T ）
> **参考答案**：T

11. CHAR 类型的字符串长度是固定的，VARCHAR 类型的字符串长度是可变的（T ）
> **参考答案**：T

12. DELETE 语句删除的数据可以通过 ROLLBACK 恢复（ T）
> **参考答案**：T (前提是开启了事务)

13. MySQL 中，一张表可以有多个主键（F ）
> **参考答案**：F (只能有一个主键，但可以是联合主键)

14. 触发器可以在 INSERT、UPDATE、DELETE 操作时触发（T ）
> **参考答案**：T

**15. ❌ [重点复习] 视图存储实际数据，修改视图会影响基础表（T ）**
> **参考答案**：F
> **解析**：视图不存数据！它只是逻辑窗口。

16. 索引越多，查询速度越快（F ）
> **参考答案**：F (写操作会变慢)

17. MySQL 中，INT 类型的字段默认长度是 11（F ）
> **参考答案**：T (通常INT(11)) **注：你的答案F需确认**

18. 事务的四大特性包括原子性、一致性、隔离性和持久性（ T）
> **参考答案**：T

19. 左外连接会返回左表所有记录和右表匹配的记录（ T）
> **参考答案**：T

20. 存储函数可以有返回值，存储过程不能有返回值（ T）
> **参考答案**：F (存储过程可以通过OUT参数返回值，虽然不是return)

21. MySQL 中，默认的端口号是 3306（T ）
> **参考答案**：T

22. 检查约束可以确保字段值符合指定的条件（ T）
> **参考答案**：T

23. root用户作为系统管理员，拥有MySQL数据库的所有操作权限（T ）
> **参考答案**：T

**24. ❌ [重点复习] SELECT语句中，DISTINCT关键字用于保留结果集中的重复行（T ）**
> **参考答案**：F
> **解析**：DISTINCT 是**去重**，去掉重复的。

25. MySQL 中，用户创建后默认拥有所有数据库的操作权限（ F）
> **参考答案**：F (只有USAGE权限)

26. 二进制日志记录了数据库中所有更改数据的语句（T ）
> **参考答案**：T

27. 全外连接会返回两个表中所有的记录，无论是否匹配（T ）
> **参考答案**：T

28. 存储过程可以在其他存储过程中调用（ T）
> **参考答案**：T

29. MySQL 中，TEXT 类型的字段可以存储任意长度的文本（F ）
> **参考答案**：F (有最大限制)

**30. ❌ [重点复习] 事务提交后，数据修改将永久保存，无法通过 ROLLBACK 恢复（F ）**
> **参考答案**：T
> **解析**：这是事务的“持久性”。提交了就不能回滚了。

31. 索引可以提高查询速度，但会降低写入操作的速度（ T）
> **参考答案**：T

32. DROP DATABASE 语句可以删除数据库及其包含的所有表（T ）
> **参考答案**：T

33. 视图可以基于多个表创建（T ）
> **参考答案**：T

34. 外键约束用于保证表间的参照完整性，要求外键值必须在参照表中存在（T ）
> **参考答案**：T (或为NULL)

35. 视图不能被索引（F ）
> **参考答案**：T (一般视图不能建索引，物化视图除外，MySQL主要是T)

---

## 三、 填空题

**1. ❌ [重点复习] MySQL 中，__________类型用于存储日期和时间的组合。**
   * 你的答案：DATATIME
   * **参考答案**：`DATETIME` (注意拼写，中间没有A)

2. 在 CREATE TABLE 语句中，使用__________关键字来指定字段的默认值。
   * 你的答案：DEFAULT
   * **参考答案**：`DEFAULT`

**3. ❌ [重点复习] 数据库系统的核心是__________。**
   * 你的答案：innoDB
   * **参考答案**：`DBMS` (数据库管理系统)

4. MySQL 中，__________存储引擎支持事务和行级锁。
   * 你的答案：innoDB
   * **参考答案**：`InnoDB`

5. 用于删除表中满足条件的记录的语句是__________。
   * 你的答案：delete
   * **参考答案**：`DELETE`

6. 视图的创建语句是__________。
   * 你的答案：create
   * **参考答案**：`CREATE VIEW`

7. 数据库设计的五个阶段包括需求分析、概念结构设计、逻辑结构设计、物理结构设计和__________。
   * 你的答案：数据库实现阶段
   * **参考答案**：`数据库实施阶段` (或 数据库运行和维护)

**8. ❌ [重点复习] MySQL 中，用于查看当前打开的数据库的语句是__________。**
   * 你的答案：show database()
   * **参考答案**：`SELECT DATABASE()`

9. MySQL安装成功后，在系统中会默认建立一个__________用户。
   * 你的答案：root
   * **参考答案**：`root`

**10. ❌ [重点复习] 用于修改表字段默认值的语句是__________。**
   * 你的答案：alert
   * **参考答案**：`ALTER` (注意拼写)

11. MySQL 中，字符串类型分为固定长度和可变长度，其中__________类型是可变长度字符串。
    * 你的答案：varchar
    * **参考答案**：`VARCHAR`

12. 在MySQL的安装过程中，若选用“启用TCP/IP网络”，则MySQL会默认选用的端口号是__________。
    * 你的答案：3306
    * **参考答案**：`3306`

13. 外键约束的作用是__________。
    * 你的答案：保持数据一致性
    * **参考答案**：`保持数据一致性` (或 参照完整性)

14. MySQL 中，用于查看表中所有记录的语句是__________。
    * 你的答案：select
    * **参考答案**：`SELECT * FROM 表名`

15. 存储过程的创建语句是__________。
    * 你的答案：create
    * **参考答案**：`CREATE PROCEDURE`

16. 在MySQL中，通常使用_________值来表示一个列植没有值或缺值的情形。
    * 你的答案：(空)
    * **参考答案**：`NULL`

17. 用于在表中插入一条记录的语句是__________。
    * 你的答案：insert into
    * **参考答案**：`INSERT INTO`

18. MySQL 中，用于删除索引的语句是__________。
    * 你的答案：drop
    * **参考答案**：`DROP INDEX` (或 ALTER TABLE ... DROP INDEX)

19. 事务的隔离性是指__________。
    * 你的答案：一个事务不被其他事务干扰
    * **参考答案**：`一个事务不被其他事务干扰`

**20. ❌ [重点复习] 用于修改表名的语句是__________。**
    * 你的答案：(空)
    * **参考答案**：`RENAME TABLE` (或 `ALTER TABLE ... RENAME ...`)

**21. ❌ [重点复习] 数据库的三级模式结构包括外模式、模式和__________。**
    * 你的答案：(空)
    * **参考答案**：`内模式`

22. 在CREATE TABLE语句中，通常使用__________关键字来指定主键。
    * 你的答案：primary key
    * **参考答案**：`PRIMARY KEY`

23. 索引的创建语句是__________。
    * 你的答案：create index to
    * **参考答案**：`CREATE INDEX ... ON ...`

24. 事务的原子性是指__________。
    * 你的答案：事务最小单位
    * **参考答案**：`事务是不可分割的最小工作单位`
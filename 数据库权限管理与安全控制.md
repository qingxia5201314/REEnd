# 数据库权限管理与安全控制
- root用户hi系统管理员，拥有操作MySQL数据库的所有权限
- 普通用户只有创建该用户时所赋予的权限

1. 创建最简单的用户
   1.  create user 'Kitepop' identified by 'kali';
2. 创建普通账号：user2,主机:localhost，密码：123456
   1. create user 'Miku'@'localhost' identified by 'qingxia520';
3. 创建普通账号：user3,127.0.0.1,密码：123456
   1. create user 'QX'@'127.0.0.1' identified by 'kali';

## 登陆测试
- 五个参数
  - -h
  - -uroot
  - -P
  - -p
  - -Db240205
- mysql -h127.0.0.1 -uroot -P3303 -p -Db240205

## 普通用户修改自己的密码
- 普通账号只能修改自己的密码
- 管理员账户可以修改自己和普通用户的密码
- alter user user() identified by 'kali';

## 查看当前登陆账号
- select current_user();

## root另一种修改自己密码的方式
- 不需要登陆
- mysqladmin -uroot -h127.0.0.1 -pkali password 123

## 登陆之后,root修改其他用户
- alter user username identified by 'kali';


## 使用update 命令，将用户名1重新改为用户名2
- ```
  update mysql.user set user='Qingxia' where user='QX';
  flush privileges;
  ```

用管理员root修改普通用户账户名
1. rename user 'user'@'127.0.0.1' to 'user'@'127.0.0.1';
2. ```
    update mysql.user
    set user='user3'
    where user='user4';
    刷新权限：
    flush privileges;
    ```
## 普通账号的删除
1. drop语法
     - drop user 'user'@'localhost';
  2. 批量删除mysql.user表数据
      - delete from mysql.user where user in('user1','user3');

---

## 权限授予与回收
### 授权
- 授权给user1@127.0.0.1 使其只能查看数据库b240205中emp表中的ename,job字段
  - grant select (empno,ename,job) on b240205.emp to user1@127.0.0.1;
- 授权全部表权限
  - grant all privileges on b240205.* to user1@127.0.0.1;
- 授权全部权限
  - grant all privileges on \*.* to user1@127.0.0.1;
- 授予账户给其他账户授予权限的权限
  - grant all privileges on \*.* to user1@127.0.0.1 with grant option;

### 回收
- 查看拥有的权限
  - show grants for kite@localhost;
- 收回权限
  - revoke all on *.* from kite@localhost
- 收回数据库上的select权限
  - revoke select on jxgl.* from kite@localhost;
- 授予操作jxgl数据库的insert\update\delete权限
  - grant insert,update,delete on jxgl.* to kite;
- 创建角色
  - create role '[name]'
- 将角色授予给权限
  - grant '[1]' to 'kite'@'localhost';
- 将用户所有角色设置为默认角色
  - set default role all to 'kite'@'localhost';

### 删除角色
- drop role []